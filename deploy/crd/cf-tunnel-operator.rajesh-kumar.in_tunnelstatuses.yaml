---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: tunnelstatuses.cf-tunnel-operator.rajesh-kumar.in
spec:
  group: cf-tunnel-operator.rajesh-kumar.in
  names:
    kind: TunnelStatus
    listKind: TunnelStatusList
    plural: tunnelstatuses
    singular: tunnelstatus
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              httpRouteName:
                type: string
              httpRouteNamespace:
                type: string
            required:
            - httpRouteName
            - httpRouteNamespace
            type: object
          status:
            properties:
              appid:
                type: string
              backendService:
                type: string
              hostname:
                type: string
              lastSyncTime:
                format: date-time
                type: string
              message:
                type: string
              notlsverify:
                type: boolean
              policyid:
                type: string
              scheme:
                type: string
              syncStatus:
                type: string
            required:
            - backendService
            - message
            - notlsverify
            - scheme
            - syncStatus
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
